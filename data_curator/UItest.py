#!/usr/bin/env python3

from PyQt5.QtCore import *
from PyQt5.QtWidgets import *
# Clean up once we know which parts we want

import os

startdir = os.getcwd()

# Frame with horizontal layout
# Right side: Graphic with bar underneath
# Ride side under-bar: play - stop
# Right side under-bar 2: modify buttons FSBLNR-MX (modify, exclude)
# Left side: file list
# Left side top: path shooser
# Left side top 2: filename filter
# Left side middle: list of files
# Left side bottom: filename filter if not upder path chooser


